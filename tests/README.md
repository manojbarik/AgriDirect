# Test Workspace

Cross-component and end-to-end tests will be added here as workflows are implemented. Component-specific tests live beside their workspace:

- `backend/tests` for FastAPI and Python tests.
- `frontend/src` or `frontend/tests` for frontend tests.
- `ml/tests` for model and data-pipeline tests.
